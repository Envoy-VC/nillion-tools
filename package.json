{
  "name": "web3-turbo-starter",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier . --write --cache",
    "release": "pnpm turbo build --filter=nillion-key-manager && changeset publish",
    "version-packages": "changeset version",
    "changeset": "changeset"
  },
  "devDependencies": {
    "config": "workspace:*",
    "@changesets/cli": "^2.27.1",
    "prettier": "^3.2.5",
    "turbo": "^2"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@wagmi/cli": "^2.1.8"
  }
}
